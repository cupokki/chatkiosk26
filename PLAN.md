# AI 키오스크 기반 스마트 주문 및 관리 시스템

## 1. 프로젝트 개요

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | AI 키오스크 기반 스마트 주문 및 관리 시스템 |
| 진행 방식 | 1박 2일 타임어택 솔로 해커톤 |
| 목표 | LangChain 기반 자연어 주문 보조와 QueryDSL 기반 동적 조회를 갖춘 동작 가능한 MVP 완성 |
| 원칙 | 핵심 흐름을 우선 완성하고, 인증·실결제·재고 등 부가 기능은 구현하지 않는다. |

## 2. 문제와 해결 방식

일반 키오스크는 고객이 메뉴를 직접 탐색해야 하고, 관리자는 다양한 조건을 조합해 메뉴와 판매 현황을 확인하기 어렵다.

이 프로젝트는 고객의 자연어 요청을 주문 조건으로 변환해 메뉴를 추천하고, 관리자가 복합 조건으로 메뉴와 판매 통계를 조회할 수 있는 최소 시스템을 구현한다.

## 3. MVP 성공 기준

아래 세 가지가 모두 가능한 상태를 MVP 완료로 정의한다.

1. 고객이 자연어로 요청하면 시스템이 조건을 추출하고, 해당 조건에 맞는 메뉴를 추천한다.
2. 고객이 추천 또는 메뉴 목록에서 장바구니에 담아 주문을 완료할 수 있다. 결제는 즉시 성공하는 스텁으로 처리한다.
3. 관리자가 복합 조건으로 메뉴를 검색하고, 기간·카테고리 조건에 따른 매출과 판매 수량을 조회할 수 있다.

## 3.1 역할 분담과 개발 원칙

| 역할 | 담당 범위 |
| --- | --- |
| 개발자 | 백엔드 전반: Spring Boot, JPA·QueryDSL, API 설계, 데이터베이스, LangChain 비즈니스 로직, Docker 환경 |
| Codex | 프론트엔드의 초기 구조와 화면 골격, 라우팅, 반복 UI·API 연결 코드 작성 지원 |

프론트엔드는 Codex를 중심으로 빠르게 골격화하되, 개발자가 API 계약·데이터 형식·핵심 사용자 흐름을 검토하고 최종 결정한다. Codex에는 한 번에 큰 화면 전체를 맡기기보다, 화면·컴포넌트·API 연결 단위로 명확한 입력과 완료 조건을 전달한다.

## 4. 핵심 사용자 시나리오

### 4.1 고객 주문

1. 고객이 “맵지 않고, 혼자 먹기 좋으며 1만 원 이하인 메뉴를 추천해줘”라고 입력한다.
2. AI가 요청을 구조화된 조건으로 해석한다.
3. 조건에 맞는 메뉴 목록을 추천한다.
4. 고객이 메뉴를 장바구니에 담고 수량을 조정한다.
5. 고객이 주문을 완료한다. 결제는 성공 스텁으로 처리하고 주문 상태는 `COMPLETED`가 된다.

### 4.2 관리자 메뉴·통계 조회

1. 관리자가 메뉴 이름, 카테고리, 판매 여부, 가격 범위를 조합해 메뉴를 검색한다.
2. 관리자가 주문 기간과 카테고리를 지정해 매출·판매 수량·인기 메뉴를 조회한다.

## 5. 기능 범위

### 5.1 고객 클라이언트

| 기능 | MVP 범위 |
| --- | --- |
| AI 채팅 | 자연어 요청 입력, 조건 해석 결과와 추천 메뉴 표시 |
| 메뉴 탐색 | 전체 메뉴 조회 및 기본 카테고리 필터 |
| 장바구니 | 메뉴 추가·삭제·수량 변경·합계 표시 |
| 주문 | 주문 생성 및 즉시 성공 결제 스텁 |

### 5.2 관리자 콘솔

| 기능 | MVP 범위 |
| --- | --- |
| 메뉴 관리 | 메뉴 등록·수정·삭제, 판매 여부 변경 |
| 동적 메뉴 검색 | 키워드, 카테고리, 판매 여부, 최소·최대 가격의 조합 검색 |
| 판매 통계 | 주문 기간, 카테고리, 가격 구간 필터; 매출 합계·판매 수량·인기 메뉴 Top 5 |

### 5.3 MVP 제외 항목

- 회원가입, 로그인, 권한 관리
- 실제 PG 결제, 환불
- 재고 관리, 쿠폰, 포인트
- 이미지 업로드와 파일 저장소
- 다점포·다중 관리자 기능
- 운영 수준의 관측성 시스템(Prometheus, Grafana)

## 6. 핵심 기술 설계

### 6.1 LangChain: 자연어 조건 추출과 추천

LangChain은 주문을 직접 확정하지 않는다. 고객의 문장을 메뉴 검색에 쓸 수 있는 조건으로 구조화하고, 백엔드의 메뉴 조회 결과를 추천에 사용한다.

예시 요청:

> “맵지 않고 혼자 먹기 좋으며 1만 원 이하인 메뉴를 추천해줘.”

예시 추출 결과:

```json
{
  "category": "MEAL",
  "maxPrice": 10000,
  "excludeTags": ["spicy"],
  "partySize": 1
}
```

실패 처리:

- LLM 호출 또는 JSON 파싱에 실패하면 이해하지 못한 조건을 안내하고 재입력을 요청한다.
- 일반 메뉴 목록과 장바구니 주문은 AI 기능 장애와 무관하게 이용 가능해야 한다.
- API 키가 없을 경우를 대비해 간단한 키워드 기반 조건 추출 모드를 둔다.

### 6.2 QueryDSL: 동적 조회

동적 쿼리는 아래의 선택 조건들을 조합해 처리한다. 조건이 비어 있으면 해당 조건은 적용하지 않는다.

| 조회 | 조건 | 결과 |
| --- | --- | --- |
| 메뉴 검색 | 키워드, 카테고리, 판매 여부, 가격 범위 | 페이징된 메뉴 목록 |
| 판매 통계 | 주문일 범위, 카테고리, 가격 구간 | 매출 합계, 판매 수량, 인기 메뉴 Top 5 |

통계 집계 기준은 주문 상태가 `COMPLETED`인 주문으로 한정한다.

## 7. 데이터 모델 초안

| 엔티티 | 주요 필드 | 설명 |
| --- | --- | --- |
| `Category` | id, name | 메뉴 분류 |
| `Menu` | id, name, price, description, tags, available, categoryId | 판매 메뉴 |
| `Order` | id, totalAmount, status, orderedAt | 주문 헤더 |
| `OrderItem` | id, orderId, menuId, menuName, unitPrice, quantity | 주문 당시 메뉴 스냅샷 |

- `tags`는 초기에는 문자열 또는 간단한 문자열 컬렉션으로 구현한다.
- 주문 항목에는 주문 시점의 메뉴명과 가격을 저장한다. 이후 메뉴를 수정해도 과거 통계가 변하지 않아야 한다.
- 주문 상태는 MVP에서 `COMPLETED`만 사용한다.

## 8. API 초안

| 구분 | 메서드·경로 | 설명 |
| --- | --- | --- |
| 고객 | `POST /api/ai/recommendations` | 자연어 요청을 해석해 추천 메뉴 반환 |
| 고객 | `GET /api/menus` | 고객용 메뉴 목록 조회 |
| 고객 | `POST /api/orders` | 장바구니 항목으로 주문 완료 |
| 관리자 | `GET /api/admin/menus` | 복합 조건 메뉴 검색 |
| 관리자 | `POST /api/admin/menus` | 메뉴 등록 |
| 관리자 | `PATCH /api/admin/menus/{id}` | 메뉴 수정 또는 판매 상태 변경 |
| 관리자 | `DELETE /api/admin/menus/{id}` | 메뉴 삭제 |
| 관리자 | `GET /api/admin/statistics/sales` | 기간·카테고리 조건 판매 통계 |

## 9. 기술 구성과 로컬 실행

| 영역 | 선택 |
| --- | --- |
| Backend | Java 21, Spring Boot, Spring Data JPA, QueryDSL, LangChain4j 또는 Spring AI |
| Frontend runtime | Node.js LTS |
| Frontend framework | React, JavaScript, Vite |
| Frontend routing | React Router |
| HTTP client | Axios |
| UI styling | Tailwind CSS만 사용. 별도 UI 컴포넌트 라이브러리는 사용하지 않음. |
| Chart | Recharts |
| Icons | Lucide React |
| Frontend quality | Vite 기본 ESLint 설정 |
| Database | PostgreSQL |
| Infrastructure | Docker, Docker Compose |
| API 문서 | Swagger/OpenAPI |

프론트엔드 구현 제한:

- 상태 관리는 React의 `useState`, `useContext`만 사용한다. 별도 상태 관리 라이브러리는 도입하지 않는다.
- 서버 통신은 Axios로 통일한다.
- Tailwind CSS 외 UI 라이브러리(shadcn/ui, Material UI, Bootstrap 등)는 사용하지 않는다.
- 고객 주문 화면과 관리자 콘솔은 React Router 경로로 구분한다.

권장 모노레포 구조:

```text
.
├── backend/
├── frontend/
├── docker-compose.yml
├── .env.example
└── PLAN.md
```

`docker compose up`으로 데이터베이스와 백엔드를 실행할 수 있어야 한다. 프론트엔드는 개발 서버 실행을 허용하되, 최종 시연 전 통합 실행 여부를 점검한다.

## 10. 사전 준비 체크리스트

- [ ] Java 21과 Gradle 실행 확인
- [ ] Node.js LTS와 패키지 매니저 실행 확인
- [ ] React(JavaScript)·Vite 프로젝트 및 ESLint 동작 확인
- [ ] React Router, Axios, Tailwind CSS, Recharts, Lucide React 설치
- [ ] Docker Desktop 실행 확인
- [ ] PostgreSQL 접속 정보와 환경 변수 정의
- [ ] 사용할 LLM 제공자와 API 키 준비
- [ ] `.env.example`에 비밀값 없는 변수명·예시값 작성
- [ ] 메뉴 20개 이상, 완료 주문 100건 이상의 시드 데이터 준비
- [ ] Swagger 또는 API 요청 예시 준비

## 11. 1박 2일 실행 계획

### Day 1 — 주문 흐름 완성

1. 모노레포, Docker Compose, 데이터베이스, 시드 데이터 구성
2. 메뉴·주문 도메인과 고객용 API 구현
3. Codex로 고객 주문 화면, 라우팅, 장바구니 UI 골격 구현
4. 주문 생성·결제 스텁까지 연결
5. 자연어 조건 추출과 추천 API의 기본 흐름 연결

### Day 2 — 관리자 조회와 시연 완성

1. QueryDSL 메뉴 검색 구현
2. 판매 통계 쿼리와 관리자 대시보드 구현
3. LLM 실패 처리와 키워드 기반 대체 흐름 추가
4. 시드 데이터와 주요 API 검증
5. Docker 실행, 데모 시나리오, README 정리

## 12. 데모 순서

1. 고객 화면에서 자연어로 메뉴를 추천받는다.
2. 추천 메뉴를 장바구니에 담고 주문을 완료한다.
3. 관리자 화면에서 방금 주문한 메뉴를 포함하도록 복합 조건 검색을 실행한다.
4. 기간·카테고리 필터를 바꾸며 매출과 판매량, 인기 메뉴 통계를 확인한다.

## 13. Post-MVP

- LLM 호출·파싱 결과·실패율 로깅
- 동적 쿼리 실행 시간과 슬로우 쿼리 추적
- Prometheus와 Grafana 연동
- 인증·권한, 실결제, 재고 관리 도입
